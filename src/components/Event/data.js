import {
  Calendar,
  Users,
  TrendingUp,
  Heart,
  UserPlus,
  CreditCard,
} from "lucide-react";

// All mock data has been removed to use API data instead
// Sample events data - now empty array to be replaced with API data
export const sampleEvents = [];
